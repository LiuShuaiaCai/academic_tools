# 2026-05-04 工作日志

## submissions 页面改造

- 修复投稿时间线添加保存失败（缺少 onTLNewDateChange/onTLNewEventChange/onTLNewRemarkInput）
- 修复备注字段(note)不保存的问题（saveForm 的 data 对象缺少 note）
- 修复 iOS 日期解析兼容性（new Date("yyyy-mm-dd HH:mm:ss") 在 iOS 不支持），在 dbInit.js 新增 parseDate 工具函数并全局替换
- 实现投稿列表服务端分页（每页20条，上拉加载更多），按 deadline 升序排序（null 沉底）
- 实现高级搜索功能：状态/身份/期刊/优先级/到期时间区间，默认隐藏，点击"高级筛选"展开
  - 各选项从用户记录中动态统计去重，显示每个选项数量（如"已投稿 (3)"）
  - 筛选条件联动：每个维度的选项数量基于"排除自身维度的其他筛选结果"重新统计
  - 核心函数：applyFilter（内含 applyFilters 局部函数）、_buildAdvOptionsFromBases、buildAdvOptions
