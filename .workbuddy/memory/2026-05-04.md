# 投稿管理模块拆分记录 - 2026-05-04

## 拆分完成

将原来的 `submissions` 页面（列表+表单混合）拆分为：
- **列表页**：`submissions/submissions.*` - 只保留列表、搜索、筛选逻辑
- **表单组件**：`submissions/form/` - 独立的表单弹窗组件

## 新增文件
1. `utils/submissions-config.js` - 常量、选项数据
2. `utils/submissions-format.js` - 格式工具函数（formatItem 等）
3. `submissions/form/form.json` - 组件配置
4. `submissions/form/form.js` - 组件逻辑（Component 构造器）
5. `submissions/form/form.wxml` - 组件模板
6. `submissions/form/form.wxss` - 组件样式

## 修改文件
1. `submissions/submissions.json` - 引入 form 组件
2. `submissions/submissions.wxml` - 删除表单弹层，添加 `<submission-form />` 组件，修复 `wx:for="{{3}}"` 问题
3. `submissions/submissions.js` - 删除所有表单逻辑，只保留列表相关，添加 `onFormSave`/`onFormCancel` 处理组件事件

## 已知问题待测试
1. `formatItem` 中的 `formatTimelineForDisplay` 调用可能需要传入 `tlEventOptions`
2. form.js 中 `this.data.isEdit` vs `this.properties.isEdit` 的用法
3. form.wxml 中可能有重复内容（第288-290行附近）

## 下一步
需要在微信开发者工具中测试，确认：
1. 列表加载正常
2. 新建投稿弹窗正常显示和保存
3. 编辑投稿弹窗正常加载和保存
4. 无渲染层错误

## 2026-05-04 晚间更新

### 投稿列表卡片截止日期显示逻辑优化
- 文件：`utils/submissions-format.js` 第59行 `overdueLabel`
- 新逻辑：
  - **已过期**（overdue < 0）：显示「超过X天」
  - **剩余0-3天**（0 ≤ overdue ≤ 3）：显示「剩余X天」
  - **剩余>3天**（overdue > 3）：显示到期日期（格式：YYYY-MM-DD）
- 原逻辑：只区分已超期/剩余天数，无日期展示
