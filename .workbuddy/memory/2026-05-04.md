# 2026-05-04 工作记录

## 投稿管理 - 高级筛选优化

1. **每个条件加 ✕ 清空按钮**：状态/身份/期刊/优先级/完成状态/到期时间各加独立 x 按钮
2. **自动搜索**：选条件或清空条件后自动触发 loadList 服务端查询，移除"应用筛选"按钮
3. **搜索框保留**：高级筛选变化时不清空 searchKeyword，搜索内容一起带进查询
4. **快速筛选互斥**：高级筛选操作时清空 quickFilter，避免两套筛选体系冲突
5. 高级筛选内部各条件联动，可组合查询

## 投稿管理 - 统计数字与搜索框联动

1. **云函数修改**：`submissionStats` 支持 `keyword` 参数，搜索时只统计匹配记录
2. **前端修改**：`loadStats()` 传入 `searchKeyword`，移除「无搜索词才调用」限制
3. **效果**：搜索时四个标签数字（全部/未完成/急需处理/特别紧急）动态显示搜索结果对应状态的数量
