# 2026-05-06 工作日志

## 会议管理模块重构完成
- 列表页 + 表单组件架构（参照 reviews 模块）
- 新增工具库：conferences-config.js、conferences-format.js、conferences-form.wxss
- 新增表单组件：pages/conferences/form/form.{js,wxml,wxss,json}
- 云函数 academicAPI 新增 conferenceStats action（total/pending/near/registered）

## 熟悉项目结构
- 全面梳理了 academic_tools 微信小程序项目架构（11个页面、3个云函数、6个工具库）
- 重点摸清了 reviews 模块的 AI 审稿、审稿模板、决定弹窗等核心逻辑

## 审稿决定弹窗配置驱动化
- `reviews-config.js` 的 `DECISION_OPTIONS` 单一来源控制两处弹窗
- `form.wxml` 决定弹窗改为 `wx:for` 循环渲染，硬编码移除
- `form.wxss` 新增 `.decision-options-grid` 样式

## 首页工具卡片 isTaskType 机制
- `academicTools` 初始化工具数据增加 `isTaskType` 字段
  - true: submission/review/conference（任务型，显示"X 任务待完成"）
  - false: archive/note/citation/journal/achievement（存储型，不显示）
- `academicAPI` 的 `getAllTools` 增加缺省推断逻辑，无 `isTaskType` 时自动按 id 判断
- `home.js`/`home.wxml` 根据 `isTaskType` 控制是否渲染统计区
- `academicTools` 中 `initTools()` upsert 时同步保存 `isTaskType`

## 资料归档页面优化
- 点击 FAB 后先弹 `wx.showActionSheet` 选择分类（投稿/审稿/会议/其他）
- 选完再调 `wx.chooseMessageFile`，上传时 category 用用户选择的，不再按扩展名自动分类
- 删除废弃的 `getCategoryByExt` 方法
- 去掉「图片」分类（选项卡、categoryOptions、counts 均移除）
- 横线从搜索栏移至分类选项卡底部

## 资料归档上传弹窗优化
- 新增 `loadCategoriesWithDataCheck()` 函数，并行检查每个分类的文件数量
- 删除分类时检查 `hasData` 状态：有文件的分类显示"有数据"按钮且不可点击
- 分类管理弹窗中，有数据的分类显示灰色"有数据"按钮，无数据的才显示红色"删除"按钮
