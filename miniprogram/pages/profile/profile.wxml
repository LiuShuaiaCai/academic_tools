<!--pages/profile/profile.wxml-->
<view class="profile-page">
  <!-- 头部 -->
  <view class="profile-header">
    <view class="avatar-area">
      <image class="profile-avatar-img" src="{{avatar}}" mode="aspectFill" wx:if="{{isWechatAvatar}}"></image>
      <text class="profile-avatar" wx:else>{{avatar}}</text>
      <view class="avatar-edit-icon">📷</view>
      <button class="avatar-btn-overlay" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar"></button>
    </view>
    <view class="profile-info">
      <input type="nickname" value="{{nickname}}" placeholder="点击设置昵称" bindchange="onNicknameInput" class="nickname-input"/>
      <view class="profile-role-tag">{{roleLabels[userRole]}}</view>
    </view>
  </view>

  <!-- 积分条 -->
  <view class="credits-bar" bindtap="goToCredits">
    <view class="credits-bar-item">
      <text class="credits-bar-icon">💰</text>
      <text class="credits-bar-num">{{credits}}</text>
      <text class="credits-bar-label">积分</text>
    </view>
    <view class="credits-bar-divider"></view>
    <view class="credits-bar-item">
      <text class="credits-bar-icon">🔥</text>
      <text class="credits-bar-num">{{continuousDays}}</text>
      <text class="credits-bar-label">连续签到</text>
    </view>
    <view class="signin-btn-bar {{signedToday ? 'signed' : ''}}" catchtap="doSignin">
      <text wx:if="{{signedToday}}">✓已签到</text>
      <text wx:else>签到</text>
    </view>
  </view>

  <!-- 统计数据 -->
  <view class="profile-stats" wx:if="{{statItems.length > 0}}">
    <block wx:for="{{statItems}}" wx:key="id">
      <view class="pstat-item" bindtap="goToStatPage" data-id="{{item.id}}" data-page="{{item.pagePath}}" hover-class="pstat-item-active">
        <view class="pstat-num {{item.color}}">{{item.count}}</view>
        <view class="pstat-label">{{item.name}}</view>
      </view>
      <view class="pstat-divider" wx:if="{{index < statItems.length - 1}}"></view>
    </block>
  </view>

  <!-- 功能模块 -->
  <view class="menu-group">
    <view class="menu-item" bindtap="goToEditProfile">
      <view class="menu-icon-box icon-blue icon-user"></view>
      <text class="menu-text">完善资料</text>
      <text class="profile-badge" wx:if="{{!profileCompleted}}">送50积分</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="goToToolManager">
      <view class="menu-icon-box icon-cyan icon-tools"></view>
      <text class="menu-text">工具管理</text>
      <text class="menu-badge" wx:if="{{statItems.length > 0}}">已启用{{statItems.length}}项</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="goToSettings">
      <view class="menu-icon-box icon-gray icon-settings"></view>
      <text class="menu-text">设置</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="sendFeedback">
      <view class="menu-icon-box icon-yellow icon-chat"></view>
      <text class="menu-text">意见反馈</text>
      <text class="menu-arrow">›</text>
    </view>
  </view>

  <!-- 账户操作 -->
  <view class="menu-group">
    <view class="menu-item" bindtap="resetOnboarding">
      <view class="menu-icon-box icon-orange icon-refresh"></view>
      <text class="menu-text">重新设置角色</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="clearData">
      <view class="menu-icon-box icon-gray icon-trash"></view>
      <text class="menu-text">清除本地数据</text>
      <text class="menu-arrow">›</text>
    </view>
    <view class="menu-item" bindtap="showAbout">
      <view class="menu-icon-box icon-gray icon-info"></view>
      <text class="menu-text">关于</text>
      <text class="menu-arrow">›</text>
    </view>
  </view>

  <!-- 退出登录按钮 -->
  <view class="logout-btn" bindtap="logout">
    <text>退出登录</text>
  </view>


</view>
