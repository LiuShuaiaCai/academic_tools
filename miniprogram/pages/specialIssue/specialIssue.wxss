/* pages/specialIssue/specialIssue.wxss V5 */

/* 主题色由 JS 动态注入 CSS 变量到根 view，此处不硬编码 */

/* ========== 基础布局 ========== */
.page {
  min-height: 100vh;
  background: #F5F7FA;
  padding-bottom: 40rpx;
}

/* ========== 搜索区域 ========== */
.search-section {
  padding: 32rpx;
  background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
}

.search-header {
  margin-bottom: 20rpx;
}

.search-title {
  font-size: 40rpx;
  font-weight: 700;
  color: #FFF;
  margin-bottom: 8rpx;
}

.search-subtitle {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.75);
}

.search-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.search-input-wrap {
  flex: 1;
  background: #FFF;
  border-radius: 12rpx;
  padding: 0 24rpx;
}

.search-input {
  height: 72rpx;
  font-size: 26rpx;
  color: #1F2937;
}

.search-btn {
  flex-shrink: 0;
  padding: 0 24rpx;
  height: 72rpx;
  line-height: 72rpx;
  background: rgba(255, 255, 255, 0.25);
  color: #FFF;
  font-size: 26rpx;
  font-weight: 600;
  border-radius: 12rpx;
  text-align: center;
}

.fab-btn {
  position: fixed;
  right: 40rpx;
  bottom: calc(60rpx + env(safe-area-inset-bottom));
  width: 100rpx;
  height: 100rpx;
  background: var(--primary-color);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx var(--primary-shadow);
  z-index: 50;
}

.fab-icon {
  color: #FFF;
  font-size: 48rpx;
  font-weight: 300;
  line-height: 1;
}

.credit-bar {
  margin-top: 16rpx;
  text-align: right;
}

.credit-text {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.8);
}

/* ========== 任务列表 ========== */
.list-section {
  padding: 16rpx 24rpx;
}

.task-card {
  background: #FFF;
  border-radius: 16rpx;
  padding: 28rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

.task-card:active {
  background: #F9FAFB;
}

.task-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 14rpx;
}

.task-keyword {
  font-size: 30rpx;
  font-weight: 600;
  color: #1F2937;
  flex: 1;
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.task-status {
  flex-shrink: 0;
  margin-left: 16rpx;
  padding: 6rpx 20rpx;
  font-size: 24rpx;
  border-radius: 20rpx;
  font-weight: 500;
}

.status-done {
  background: #D1FAE5;
  color: #059669;
}

.status-select {
  background: #DBEAFE;
  color: #2563EB;
}

.status-ing {
  background: #FEF3C7;
  color: #92400E;
}

.status-fail {
  background: #FEE2E2;
  color: #DC2626;
}

/* 进度条 */
.task-progress {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 12rpx;
}

.progress-bar {
  flex: 1;
  height: 8rpx;
  background: #E5E7EB;
  border-radius: 4rpx;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  border-radius: 4rpx;
  transition: width 0.3s ease;
}

.bar-start { background: linear-gradient(90deg, #F59E0B, #FCD34D); }
.bar-half  { background: linear-gradient(90deg, #F59E0B, var(--primary-color)); }
.bar-done  { background: var(--primary-color); }

.progress-text {
  font-size: 22rpx;
  color: #9CA3AF;
  width: 56rpx;
  text-align: right;
}

/* 方案预览 */
.task-preview {
  display: flex;
  align-items: baseline;
  margin-bottom: 14rpx;
}

.preview-label {
  font-size: 26rpx;
  color: #6B7280;
  flex-shrink: 0;
  margin-right: 4rpx;
}

.preview-title {
  font-size: 26rpx;
  color: #374151;
  flex: 1;
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* V6: scheme badge */
.scheme-badge {
  display: inline-block;
  padding: 4rpx 14rpx;
  background: #D1FAE5;
  color: #059669;
  font-size: 22rpx;
  font-weight: 600;
  border-radius: 8rpx;
  margin-right: 8rpx;
}

/* 失败任务预览 + 重新执行按钮 */
.task-failed {
  flex-wrap: wrap;
}

.task-failed .preview-title {
  display: block;
  word-break: break-all;
  -webkit-line-clamp: unset;
  margin-bottom: 12rpx;
}

.retry-btn {
  display: inline-block;
  padding: 8rpx 24rpx;
  background: #2563EB;
  color: #fff;
  font-size: 22rpx;
  border-radius: 8rpx;
  margin-left: auto;
}

/* 任务底部 */
.task-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 8rpx;
}

.task-time {
  font-size: 24rpx;
  color: #9CA3AF;
}

.task-delete {
  font-size: 24rpx;
  color: #EF4444;
  padding: 6rpx 16rpx;
}

/* 加载更多 */
.load-more {
  text-align: center;
  padding: 24rpx;
  font-size: 26rpx;
  color: #9CA3AF;
}

/* ========== 弹窗通用 ========== */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 100;
}

.modal-dialog {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 640rpx;
  max-height: 80vh;
  background: #FFF;
  border-radius: 24rpx;
  z-index: 101;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 32rpx 16rpx;
  border-bottom: 1rpx solid #F0F0F0;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #1F2937;
}

.modal-close {
  font-size: 36rpx;
  color: #9CA3AF;
  padding: 8rpx;
}

.modal-body {
  padding: 24rpx 32rpx;
  overflow-y: auto;
  flex: 1;
}

.modal-footer {
  display: flex;
  gap: 16rpx;
  padding: 20rpx 32rpx 32rpx;
  border-top: 1rpx solid #F0F0F0;
}

.modal-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx;
  border-radius: 14rpx;
  font-size: 28rpx;
  font-weight: 600;
}

.modal-btn.cancel {
  background: #F3F4F6;
  color: #6B7280;
}

.modal-btn.confirm {
  background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
  color: #FFF;
}

.modal-btn.confirm.disabled {
  opacity: 0.6;
}

.modal-btn.full {
  flex: 1;
}

/* ========== 表单样式 ========== */
.form-group {
  margin-bottom: 24rpx;
}

.form-label {
  font-size: 26rpx;
  font-weight: 600;
  color: #374151;
  margin-bottom: 12rpx;
}

.form-input {
  height: 80rpx;
  padding: 0 24rpx;
  background: #F9FAFB;
  border: 1rpx solid #E5E7EB;
  border-radius: 12rpx;
  font-size: 28rpx;
  color: #1F2937;
}

.form-textarea {
  width: 100%;
  height: 160rpx;
  padding: 16rpx 24rpx;
  background: #F9FAFB;
  border: 1rpx solid #E5E7EB;
  border-radius: 12rpx;
  font-size: 26rpx;
  color: #1F2937;
  box-sizing: border-box;
}

.form-credit {
  display: flex;
  justify-content: space-between;
  padding: 16rpx;
  background: #FEF3C7;
  border-radius: 12rpx;
  font-size: 24rpx;
  color: #92400E;
}

/* ========== 步骤列表 ========== */
.step-item {
  display: flex;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid #F3F4F6;
}

.step-item:last-child {
  border-bottom: none;
}

.step-icon {
  font-size: 32rpx;
  margin-right: 16rpx;
}

.step-label {
  flex: 1;
  font-size: 26rpx;
  color: #374151;
}

.step-status {
  font-size: 22rpx;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
}

.step-status.done {
  background: #D1FAE5;
  color: #059669;
}

.step-status.running {
  background: #FEF3C7;
  color: #92400E;
}

.step-status.failed {
  background: #FEE2E2;
  color: #DC2626;
}

.step-status.pending {
  background: #F3F4F6;
  color: #9CA3AF;
}

/* ========== 加载/空状态 ========== */
.loading-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 80rpx 32rpx;
}

.loading-spinner {
  width: 56rpx;
  height: 56rpx;
  border: 3rpx solid #E5E7EB;
  border-top-color: var(--primary-color);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.loading-text {
  margin-top: 20rpx;
  font-size: 26rpx;
  color: #9CA3AF;
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 100rpx 48rpx;
}

.empty-icon {
  font-size: 72rpx;
  margin-bottom: 20rpx;
}

.empty-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #374151;
  margin-bottom: 8rpx;
}

.empty-desc {
  font-size: 24rpx;
  color: #9CA3AF;
}
